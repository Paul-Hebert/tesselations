<div class="hero container" style="{{data.heroBackground}}">
  <h1 class="hero__heading">Tesselation Generator</h1>

  <p class="hero__subhead">Procedurally generated tesselations for shits, giggles, and CSS background images.</p>
</div>

</div>
<div class="listings container container--wide">
  {{#each data.tesselations}}
    <a href="/details/{{this.id}}" class="card">
      <h2 class="card__title">{{this.name}}</h2>

      
      <div class="card__graphic" style="{{this.background}};background-size: {{this.size}}px;">
        {{!-- TODO: Screen reader description --}}
      </div>
    </a>
  {{/each}}
</div>